sudo: false
language: node_js
node_js:
  - '8'
install:
  - npm i npminstall && npminstall
script:
  - npm run ci
after_script:
  - npminstall codecov && codecov
before_script:
  - mongo egg_dockerize --eval 'db.createUser({user:"egg_dockerize",pwd:"egg_dockerize",roles:["readWrite"]});'
services:
  - mongodb
